#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a,b,i;
    long x=1;
    printf("");
    scanf("%d%d",&a,&b);
    scanf("%ld",&x);
    for(i=1;i<=b;i++)
    {
        x=x*a;
    }
    printf("%ld",x);
    
    return 0;
}
